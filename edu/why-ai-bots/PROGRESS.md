# PROGRESS.md — edu/why-ai-bots

## 현재 상태 (2026-09-21)

- 기존 산출물(오프닝+20장+엔딩 HTML, narration.json 22클립, diagrams 5종, slide-visible-copy.md)을 PASS 가드 기준으로 전체 대조 검토.
- **수정 완료**: `why-ai-bots.html`의 `<title>`/커버 킥커에 남아 있던 "S-Core"/"AIStory" 템플릿 브랜딩 잔재 제거(공개 저장소 노출 위험) → "BotStory · Education track"으로 교체.
- **발견**: 나레이션 총 4,266자(22클립)는 자매 프로젝트(AIStory) 실측 TTS 속도 환산 시 약 13~16분 — README/오프닝 대사가 명시하는 "30분" 목표에 못 미침 [추정, 미실측].
  - 사용자 결정(2026-09-21): 목표 시간 문구를 낮추지 않고 **나레이션을 확장**해 실제 30분에 맞춘다.
- **완료(2026-09-21)**: 사용자가 Notion "사실 원장"·"교육본" 확장 대본(30분 보강판, 22클립)을 채팅에 직접 제공 → `narration.json`과 `why-ai-bots.html`의 `SCENES[].lines`/`OPENING_TEXT`/`ENDING_TEXT`를 전량 교체. 화면에 보이는 층(title/message/bullets/visual)은 사용자가 함께 제공한 `slide-visible-copy.md`와 100% 일치해 변경하지 않음 — 나레이션(음성)만 확장.
  - 검증: HTML `lines[]` 20개 전량과 `narration.json` 22개 항목 텍스트 바이트 단위 일치 확인, `node --check`로 JS 문법 통과.
  - **총 6,401자**(사용자 제공본은 "8,027자"로 기재 — 마크다운 서식/발표자 노트 등을 포함한 수치로 추정, 실제 SCENES `lines`만 집계하면 6,401자) [불일치, 원인 미확인]. 자매 프로젝트 실측 속도 환산 시 약 **22분** — 30분 목표에는 아직 못 미침.
- **참고, 낮은 우선순위**: 다크 테마 에셋(21/23/25/27-*-dark.png) 전량 미사용, `.v-risk` 렌더러·리스크 카드(26/27) 미사용(18장은 테이블로 대체됨) — 정리 대상이나 기능에 영향 없음.

## 다음 우선순위

1. 6,401자(~22분)와 사용자 제공본 "8,027자" 표기 간 불일치 원인 확인 — 사용자에게 재확인, 필요 시 추가 확장
2. TTS 오디오 렌더링 (콘텐츠 최종 확정 후에만 — 렌더링 재작업 방지)
3. 브라우저 실사용 검증 (스크롤·다이어그램·오디오 폴백)

## 결정 사항

- 결정: 나레이션 분량을 "30분" 목표에 맞춰 확장(표현을 낮추지 않음)
  - 이유: 사용자 명시적 선택
  - 대안: 목표 시간 표현을 실제 분량에 맞게 하향(기각)
